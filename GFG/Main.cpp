#include <bits/stdc++.h>
#include "FindAllTripletsWithSum0.cpp"
using namespace std;

int main()
{
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);

	// test case
	int t;
	cin >> t;

	Solution s;

	while (t-- != 0)
	{
		int n;
		cin >> n;
		vector<int> v(n);

		for (int i = 0; i < n; i++)
		{
			cin >> v[i];
		}

		// int k;
		// cin >> k;

		// string x, y;
		// cin >> x;
		// cin >> y;

		for (int)

		cout << endl;
	}
}